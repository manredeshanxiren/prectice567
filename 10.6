#include<stdio.h>
int main()
{
	int a, b;
	char f;
	scanf_s("%d:%d", &a, &b);
	if (a < 12)
	{
		printf_s("%d:%d AM", a, b);
	}
	else if (a > 12)
	{
		printf_s("%d:%d PM", a - 12, b);
	}
	else if (a = 12)
	{
		printf_s("%d:%d PM", a, b);
	}
	return 0;
}
